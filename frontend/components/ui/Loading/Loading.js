import * as Styled from './styled';

const Loading = () => {
  return (
    <Styled.LoadingWrapper column alignCenter>
      <Styled.LoadingCube />
    </Styled.LoadingWrapper>
  );
};

export default Loading;
