import styled from 'styled-components';
import { FlexDiv } from 'shared/styles';

export const Rating = styled(FlexDiv)`
  /* width: 7rem; */
`;
export const ButtonWrapper = styled.div`
  width: 1.5rem;
`;
